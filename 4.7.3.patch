### 1. CI Matrix `include` 块配置

为了区分不同编译器的 `CC` 和 `CXX` 环境变量，可以在 GitHub Actions 的 `matrix` 策略中定义如下 `include` 块：

```yaml
strategy:
  matrix:
    include:
      # GCC
      - { compiler: gcc, version: 12, cc: gcc-12, cxx: g++-12 }
      - { compiler: gcc, version: 13, cc: gcc-13, cxx: g++-13 }
      - { compiler: gcc, version: 14, cc: gcc-14, cxx: g++-14 }
      
      # Clang
      - { compiler: clang, version: 16, cc: clang-16, cxx: clang++-16 }
      - { compiler: clang, version: 17, cc: clang-17, cxx: clang++-17 }
      - { compiler: clang, version: 18, cc: clang-18, cxx: clang++-18 }
      
      # XCode (Apple Clang)
      - { compiler: xcode, version: '14.3.1', cc: clang, cxx: clang++ }
      - { compiler: xcode, version: '15.4', cc: clang, cxx: clang++ }
      
      # Visual Studio (MSVC)
      - { compiler: msvc, version: 2019, cc: cl, cxx: cl }
      - { compiler: msvc, version: 2022, cc: cl, cxx: cl }
      
      # Visual Studio (LLVM / Clang-CL)
      - { compiler: llvm, version: 2019, cc: clang-cl, cxx: clang-cl }
      - { compiler: llvm, version: 2022, cc: clang-cl, cxx: clang-cl }
```
*提示：在具体的 Job step 中，你可以通过 `${{ matrix.cc }}` 和 `${{ matrix.cxx }}` 将它们导出为环境变量，例如 `env: CC: ${{ matrix.cc }}`。*

### 2. FetchContent 参数与 CI 耗时

* **`GIT_TAG "v4.2.0"`**：指定 FetchContent 拉取 GSL 仓库的特定版本（这里是 `v4.2.0` 标签）。它确保了项目依赖的固定和一致性。
* **`GIT_SHALLOW ON`**：控制 Git 进行浅克隆（Shallow Clone），即只拉取 `v4.2.0` 这个节点对应的最新一次提交代码（相当于 `git clone --depth 1`），而不拉取完整的 Git 提交历史。
* **若不设置 `GIT_SHALLOW` 的影响**：如果不设置此项（默认为 `OFF`），CMake 会将 GSL 仓库的所有历史提交记录全部克隆下来。这会大大增加网络下载体积和磁盘 I/O 写入，**导致 CI 中 FetchContent 阶段的耗时显著增加**。

### 3. CTest 命令

要仅运行名称包含 `span` 的测试，并以 JSON 格式输出测试结果，可以使用如下命令（需要 CMake 3.30 及以上版本支持）：

```bash
ctest -R "span" --output-json results.json
```

*(补充说明：如果是较旧版本的 CMake，无法直接在运行测试时输出 JSON 格式的结果，通常会用 `--output-junit results.xml` 输出 XML 报告；或者使用 `--show-only=json-v1` 以 JSON 格式输出**测试列表**但不执行测试。)*