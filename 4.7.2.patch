基于 `.github/workflows/compilers.yml` 与 `README.md` 中的支持矩阵，可以把 CI 的 `matrix.include` 写成下面这种形式，用 `cc` / `cxx` 明确区分不同编译器对应的 `CC` / `CXX` 环境变量：

```yaml
strategy:
  matrix:
    build_type: [Debug, Release]
    cxx_version: [14, 17, 20, 23]
    include:
      - name: gcc-12
        os: ubuntu-24.04
        compiler_family: gcc
        cc: gcc-12
        cxx: g++-12

      - name: gcc-13
        os: ubuntu-24.04
        compiler_family: gcc
        cc: gcc-13
        cxx: g++-13

      - name: gcc-14
        os: ubuntu-24.04
        compiler_family: gcc
        cc: gcc-14
        cxx: g++-14

      - name: clang-16
        os: ubuntu-24.04
        compiler_family: clang
        cc: clang-16
        cxx: clang++-16

      - name: clang-17
        os: ubuntu-24.04
        compiler_family: clang
        cc: clang-17
        cxx: clang++-17

      - name: clang-18
        os: ubuntu-24.04
        compiler_family: clang
        cc: clang-18
        cxx: clang++-18

      - name: xcode-14.3.1
        os: macos-13
        compiler_family: apple-clang
        xcode_version: '14.3.1'
        cc: clang
        cxx: clang++

      - name: xcode-15.4
        os: macos-14
        compiler_family: apple-clang
        xcode_version: '15.4'
        cc: clang
        cxx: clang++

      - name: vs2019-msvc
        os: windows-2019
        compiler_family: msvc
        generator: 'Visual Studio 16 2019'
        toolset: ''
        cc: cl
        cxx: cl

      - name: vs2022-msvc
        os: windows-2022
        compiler_family: msvc
        generator: 'Visual Studio 17 2022'
        toolset: ''
        cc: cl
        cxx: cl

      - name: vs2019-llvm
        os: windows-2019
        compiler_family: llvm
        generator: 'Visual Studio 16 2019'
        toolset: 'ClangCL'
        cc: clang-cl
        cxx: clang-cl

      - name: vs2022-llvm
        os: windows-2022
        compiler_family: llvm
        generator: 'Visual Studio 17 2022'
        toolset: 'ClangCL'
        cc: clang-cl
        cxx: clang-cl
```

对应的步骤里可以这样使用环境变量：

```yaml
env:
  CC: ${{ matrix.cc }}
  CXX: ${{ matrix.cxx }}
```

Windows 下如果使用 Visual Studio 生成器，通常还要把生成器和工具集参数一并带给 CMake：

```yaml
with:
  extra_cmake_configure_args: >-
    ${{ matrix.toolset != '' && format('-T {0}', matrix.toolset) || '' }}
```

`FetchContent` 示例里这两个参数的含义如下：

```cmake
FetchContent_Declare(GSL
    GIT_REPOSITORY "https://github.com/microsoft/GSL"
    GIT_TAG "v4.2.0"
    GIT_SHALLOW ON
)
```

- `GIT_TAG "v4.2.0"`：指定拉取并检出的 Git 引用，这里是 GSL 的 `v4.2.0` 发布标签，作用是把依赖版本固定到该版本，避免 CI 因上游仓库继续前进而产生漂移。
- `GIT_SHALLOW ON`：告诉 CMake 以浅克隆方式获取仓库，通常只抓取目标提交附近的最小历史，减少下载体积和网络耗时。
- 如果不设置 `GIT_SHALLOW`：FetchContent 一般会抓取更多甚至完整历史，CI 在冷缓存 runner 上会有更高的网络传输和解包开销，因此拉取依赖阶段通常会更慢，整体耗时会增加。

名称中包含 `span` 的测试可以用下面的 CTest 过滤表达式：

```bash
ctest -R span
```

如果你要求的是 CTest 原生命令行的 JSON 输出格式，可写成：

```bash
ctest -R span --show-only=json-v1
```

需要注意：`--show-only=json-v1` 输出的是匹配到的测试清单 JSON，不会真正执行测试。CTest 原生命令行目前不支持“执行测试并直接把执行结果输出为 JSON”这一种单条命令；若要真正执行并导出结构化结果，原生可用的是 JUnit XML，例如：

```bash
ctest -R span --output-junit span-tests.xml
```
